# .envrc
use nix
layout node

# Add local npm binaries to PATH
PATH_add node_modules/.bin

# Set development-specific environment variables
export GNOME_SHELL_EXTENSION_DIRECTORY="$HOME/.local/share/gnome-shell/extensions"
